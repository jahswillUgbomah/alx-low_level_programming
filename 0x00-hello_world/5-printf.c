#include <stdio.h>

/**
 * main - to print out a statement
 * Return: return 0 if it prints properly or non-zero otherwise
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
