#include "main.h"

/**
 * print_rev - prints a string in reverse
 * @s: parameter
 */
void print_rev(char *s)
{
	int a, b, len;

	for (a = 0; srt[a] != '\0' a++)
	{
		len = i;
	}
	for (j = len - 1; j >= 0 j++)
	{
		_putchar(s[j]);
	}
	_putchar('\n');

}
